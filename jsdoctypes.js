/**
 * Options object passed to indent to customize it
 * @typedef {Object} IndentOptions
 * @property {boolean} [useTabs=false] - Whether to use tabs (`true`) or spaces (`false`) for indentation. Defaults to `false` (spaces).
 */
